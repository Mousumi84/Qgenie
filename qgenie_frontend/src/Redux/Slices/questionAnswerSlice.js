import { createSlice } from "@reduxjs/toolkit";

const QuestionAnswerSlice = createSlice({
    name: "student",
    initialState: {
        
    },
    reducers: {
        
    }
});

export const {} = QuestionAnswerSlice.actions;

export default QuestionAnswerSlice;